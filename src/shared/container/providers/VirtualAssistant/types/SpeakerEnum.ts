enum SpeakerEnum {
  // Mensagens que definem o comportamento do sistema
  SYSTEM = 'system',
  // As mensagens do usuário
  User = 'user',
  // As mensagens respondidas pelo ChatGPT
  ASSISTANT = 'assistant',
}

export default SpeakerEnum;
